{
	"appsUsed":[
		
	],
	"createdTime":1726657513725,
	"deleted":false,
	"deployedVersion":1,
	"deploymentState":{
		"deployedAt":1726657676404,
		"deployedBy":295,
		"deployedDefinitionId":"66eab48c3f265f0d3c262a83",
		"status":"DEPLOYED",
		"version":1,
		"workflowVersion":2
	},
	"edges":[
		{
			"fromNodeId":"UrdPv",
			"priority":0,
			"skip":false,
			"toNodeId":"XTFMw",
			"type":"next"
		},
		{
			"fromNodeId":"XTFMw",
			"priority":0,
			"skip":false,
			"toNodeId":"ybcdP",
			"type":"next"
		}
	],
	"id":"66eab3e93f265f0d3c2629e3",
	"lastModifiedBy":0,
	"lcName":"codex",
	"modifiedTime":1780496147979,
	"name":"codex",
	"nodes":[
		{
			"context":{
				"appName":"callables",
				"resourceVersion":52,
				"resourceName":"callables_from_automation"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"yUcFm-1",
			"id":"UrdPv",
			"index":0,
			"inputs":{
				"result":{
					"type":"object",
					"properties":{
						"nodes":{
							"type":"array",
							"items":{
								"type":"object",
								"properties":{},
								"additionalProperties":false
							},
							"title":"nodes"
						},
						"edges":{
							"type":"array",
							"items":{
								"type":"object",
								"properties":{},
								"additionalProperties":false
							},
							"title":"edges"
						}
					},
					"additionalProperties":false,
					"required":[]
				},
				"setup":{
					"type":"object",
					"properties":{
						"content":{
							"type":"string",
							"title":"content"
						}
					},
					"additionalProperties":false
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Trigger from automation",
			"trigger":{
				"type":"CALLABLE"
			},
			"type":"START"
		},
		{
			"context":{
				"appName":"unify_ai",
				"resourceVersion":3,
				"resourceName":"unify_ai_transform_code_to_graph",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"yUcFm-1",
			"id":"XTFMw",
			"index":0,
			"inputs":{
				"filePath":"connector_automation/tabs/automate_llm_testing.py",
				"rootPath":"ai",
				"taskId":"code_chat",
				"fileContent":"import re\nimport asyncio\nimport threading\nimport time\nimport sys\nimport gradio as gr\nfrom connector_automation.llm_providers.openai import (\n    generate_text_openai_v2,\n    async_generate_text_openai,\n)\nfrom connector_automation.prompts.llm_evaluation_prompts import *\nfrom pathlib import Path\nimport pandas as pd\nfrom gradio_client import Client\nPATTERN_0_10: re.Pattern = re.compile(r\"([0-9]+)(?=\\D*$)\")\n\"\"\"Regex that matches the last integer.\"\"\"\nPATTERN_NUMBER: re.Pattern = re.compile(r\"([+-]?[0-9]+\\.[0-9]*|[1-9][0-9]*|0)\")\n\"\"\"Regex that matches floating point and integer numbers.\"\"\"\nPATTERN_INTEGER: re.Pattern = re.compile(r\"([+-]?[1-9][0-9]*|0)\")\nimport nltk\nfrom nltk.tokenize import sent_tokenize\nimport numpy as np\nnltk.download(\"punkt\", quiet=True)\ndef validate_rating(rating) -> int:\n    \"\"\"Validate a rating is between 0 and 10.\"\"\"\n    if not 0 <= rating <= 10:\n        raise ValueError(\"Rating must be between 0 and 10\")\n    return rating\ndef re_0_10_rating(s: str) -> int:\n    \"\"\"Extract a 0-10 rating from a string.\n    If the string does not match an integer or matches an integer outside the\n    0-10 range, raises an error instead. If multiple numbers are found within\n    the expected 0-10 range, the smallest is returned.\n    Args:\n        s: String to extract rating from.\n    Returns:\n        int: Extracted rating.\n    Raises:\n        ParseError: If no integers between 0 and 10 are found in the string.\n    \"\"\"\n    if \"Score\" in s:\n        pattern = r\"Score:\\s*(\\d+)\"\n        match = re.search(pattern, s)\n        score = match.group(1)\n        try:\n            return int(score)\n        except:\n            return 5\n    matches = PATTERN_INTEGER.findall(s)\n    if not matches:\n        raise ParseError(\"int or float number\", s, pattern=PATTERN_INTEGER)\n    vals = set()\n    for match in matches:\n        try:\n            vals.add(validate_rating(int(match)))\n        except ValueError:\n            pass\n    if not vals:\n        raise ParseError(\"0-10 rating\", s)\n    # Min to handle cases like \"The rating is 8 out of 10.\"\n    return min(vals)\nIO_LOOP = None\nthread = None\ndef start_new_loop():\n    global IO_LOOP\n    IO_LOOP = asyncio.new_event_loop()\n    asyncio.set_event_loop(IO_LOOP)\n    IO_LOOP.run_forever()\n    if sys.version_info >= (3, 7):\n        pending = asyncio.all_tasks(loop=IO_LOOP)\n    else:\n        pending = asyncio.Task.all_tasks(loop=IO_LOOP)\n    coros = list()\n    for _task in pending:\n        _task.cancel()\n        coros.append(_task)\n    IO_LOOP.run_until_complete(asyncio.gather(*coros, return_exceptions=True))\ndef run_co_routine(co_routine, create_event_loop=True):\n    global thread\n    global IO_LOOP\n    try:\n        loop = asyncio.get_event_loop()\n    except RuntimeError as ex:  # No event loop in thread.\n        if create_event_loop:\n            loop = asyncio.new_event_loop()\n            asyncio.set_event_loop(loop)\n        else:\n            raise ex\n    if loop.is_running():\n        if not thread:\n            thread = threading.Thread(\n                target=start_new_loop, daemon=True\n            )  # since even sys.exit will stop only the main thread.\n            thread.start()\n            while (not IO_LOOP) or (not IO_LOOP.is_running()):\n                time.sleep(1)\n        future = asyncio.run_coroutine_threadsafe(co_routine, IO_LOOP)\n        return future.result()\n    else:\n        return loop.run_until_complete(co_routine)\ndef calculate_output(user_query, model_name):\n    if model_name == \"RAFT_V1_RAG_V15\":\n        api = \"http://34.124.205.209:8890/playground/\"\n    else:\n        api = \"http://35.247.131.198:8889/playground/\"\n    client = Client(api)\n    result = client.predict(\n        system_prompt=\"You are a helpful assistant.\",\n        prompt=user_query,\n        chatbot=[],\n        logs=\"Hello!!\",\n        provider_type=\"MLC\",\n        model_name=\"LLAMA3\",\n        temperature=0.1,\n        max_tokens=2048,\n        checkbox=[\"true\"],\n        actions_name=[],\n        api_name=\"/chat_with_llm\",\n    )\n    return result\nasync def evaluate_llm(\n    filepath, model_name, model, question_column_name, gt_column_name, progress\n):\n    df = pd.read_csv(filepath)\n    questions = df[question_column_name].to_list()\n    answers = df[gt_column_name].to_list()\n    results = []\n    progress(0, desc=\"Starting...\")\n    for i in progress.tqdm(range(len(questions))):\n        question = questions[i]\n        ground_truth = answers[i]\n        result = calculate_output(question, model_name)\n        answer = result[0][0][1]\n        # print(result[2])\n        context = result[2]\n        # context = context.split(\"\\n\")  # Output: Your  goes .\n        # context = [item for item in context if len(context) > 1]\n        # context = \"\\n\".join(context_item for context_item in context)\n        messages_context_relevance = format_context_relevance_message(question, context)\n        groundedness_scores = []\n        messages_groundedness = format_groundedness_message(context, answer)\n        messages_question_answer = format_response_relevance_message(question, answer)\n        messages_gt_answer = format_response_groundtruth_relevance_message(\n            ground_truth, answer\n        )\n        output_context_relevance_coro = async_generate_text_openai(\n            messages_context_relevance, model\n        )\n        output_groundedness_coro = async_generate_text_openai(\n            messages_groundedness, model\n        )\n        output_question_answer_coro = async_generate_text_openai(\n            messages_question_answer, model\n        )\n        output_gt_answer_coro = async_generate_text_openai(messages_gt_answer, model)\n        (\n            output_context_relevance,\n            output_groundedness,\n            output_question_answer,\n            output_gt_answer,\n        ) = await asyncio.gather(\n            output_context_relevance_coro,\n            output_groundedness_coro,\n            output_question_answer_coro,\n            output_gt_answer_coro,\n        )\n        score_context_relevance = re_0_10_rating(\n            output_context_relevance.message.content\n        )\n        groundedness_scores.append(re_0_10_rating(output_groundedness.message.content))\n        groundedness_scores = [int(score) for score in groundedness_scores]\n        score_groundedness = float(np.mean(groundedness_scores))\n        score_question_answer = re_0_10_rating(output_question_answer.message.content)\n        score_gt_answer = re_0_10_rating(output_gt_answer.message.content)\n        score_dictionary = {\n            \"question\": question,\n            \"answer\": answer,\n            \"Ground_truth\": ground_truth,\n            \"context\": context,\n            \"context_Relevance_score\": score_context_relevance,\n            \"Groundedness_score\": score_groundedness,\n            \"Question_answer_relevance_score\": score_question_answer,\n            \"Groundtruth_answer_relevance_score\": score_gt_answer,\n            \"Context_Relevance_output\": output_context_relevance,\n            \"Groundedness_output\": output_groundedness,\n            \"Question_answer_relevance_output\": output_question_answer,\n            \"Question_answer_relevance_score\": score_question_answer,\n            \"Groundtruth_answer_relevance_output\": output_gt_answer,\n        }\n        results.append(score_dictionary)\n    modified_filepath = Path(filepath).stem + \"_output.csv\"\n    df = pd.DataFrame(results)\n    df.to_csv(modified_filepath, index=False)\n    return modified_filepath\ndef upload_file(\n    file, model_name, evaluation_model, question, groundtruth, progress=gr.Progress()\n):\n    # Process the file and get the path to the modified file using the specified model\n    modified_filepath = run_co_routine(\n        evaluate_llm(\n            file.name, model_name, evaluation_model, question, groundtruth, progress\n        )\n    )\n    # Return the visibility states and the modified file path for download\n    return [\n        gr.UploadButton(visible=False),\n        gr.DownloadButton(\n            label=f\"Download {Path(modified_filepath).name}\",\n            value=modified_filepath,\n            visible=True,\n        ),\n    ]\ndef download_file():\n    # Reset the interface to allow a new upload\n    return [gr.UploadButton(visible=False), gr.DownloadButton(visible=True)]\n# Create the Gradio app\nwith gr.Blocks() as llm_evaluation_ui:\n    banking_model = gr.Dropdown(\n        label=\"Select the Banking Model\",\n        choices=[\"RAFT_V1_RAG_V15\", \"NON_RAFT_V1_RAG_V15\"],\n        type=\"value\",\n    )\n    evaluation_model = gr.Dropdown(\n        label=\"Select the Evaluation Model\",\n        choices=[\"gpt-3.5-turbo\", \"gpt-4o\"],\n        type=\"value\",\n    )\n    question = gr.Textbox(label=\"Enter the question column name\")\n    groundtruth = gr.Textbox(label=\"Enter the ground truth column name\")\n    gr.Markdown(\"Upload CSV File\")\n    with gr.Row():\n        u = gr.File(label=\"Upload a csv file\", file_types=[\".csv\"])\n        d = gr.DownloadButton(\"Download the modified file\", visible=False)\n    submit_btn = gr.Button(\"Submit\")\n    # Set up the interaction for the Submit button\n    submit_btn.click(\n        upload_file,\n        inputs=[u, banking_model, evaluation_model, question, groundtruth],\n        outputs=[u, d],\n    )\n    d.click(download_file, inputs=None, outputs=[u, d])"
			},
			"skip":false,
			"subTitle":"Unify AI",
			"title":"Transform Python code to graph",
			"type":"ACTION"
		},
		{
			"context":{
				"appName":"callables",
				"resourceVersion":48,
				"resourceName":"callables_return_to_automation",
				"type":"APPLICATION"
			},
			"debug":false,
			"dirty":false,
			"fallbackMode":"STOP",
			"groupId":"yUcFm-1",
			"id":"ybcdP",
			"index":0,
			"inputs":{
				"result":{
					"nodes":{
						"source":"{{ XTFMw.outputs.nodes }}",
						"ua:type":"mappedArray",
						"items":"{{ XTFMw.outputs.nodes[0] }}"
					},
					"edges":{
						"source":"{{ XTFMw.outputs.edges }}",
						"ua:type":"mappedArray",
						"items":"{{ XTFMw.outputs.edges[0] }}"
					}
				}
			},
			"skip":false,
			"subTitle":"Callable",
			"title":"Respond to automation",
			"type":"STOP"
		}
	],
	"ownerUserId":295,
	"settings":{
		"enableNodeLevelLogging":true,
		"enableRunLogging":true,
		"enableVariableLogging":true
	},
	"standard":false,
	"tags":[
		
	],
	"version":7
}